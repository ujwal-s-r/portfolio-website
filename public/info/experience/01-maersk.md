---

company: "A.P Moller Maersk"

role: "AIML Intern"

location: "Bangalore"

period: "Sept 2025 - Present"

order: 1

---

- Architected and built an end-to-end distributed **entity resolution system** combining **semantic retrieval**, **feature engineering**, **ML-based matching**, and **cross-encoder reranking**, with a fine-tuned cross-encoder trained on augmented data and processed across distributed GPUs in **Databricks**, improving resolution accuracy from **30% to 75%**.

- Implemented **Spark ML** pipelines to process **350M+ rows**, applying **feature engineering** and **hyperparameter tuning** to optimize ML-based matching models at scale.

- Leveraged **distributed GPUs** to efficiently process **100M+ rows** using cross-encoder inference for large-scale semantic matching and reranking.

- Optimized **Qdrant vector storage** to reduce memory requirements by **95%** while retaining **97% precision**, and built controlled **PySpark LLM pipelines** for large-scale quality validation and batch inference.

- Built **Azure Data Factory** pipelines to load **50M+ row data tables** from Data Lake into **PostgreSQL**, implementing indexing strategies and complex data-type handling while reducing data-loading time by **50%**.

- Implemented incremental pipelines to ingest **50M+ embeddings** into a graph database, enabling hybrid **Cypher + vector similarity queries** for graph-based retrieval and analysis.

- Developed a graph traversal pipeline using **PySpark GraphFrames** for efficient connected-component analysis and contributed to an **agentic AI system** orchestrating database access through tool-based agent workflows.

- Developed geographic address standardization and treatment pipelines across **300K+ addresses** to improve consistency and usability of location data.